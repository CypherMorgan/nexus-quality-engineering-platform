environment: qa
application:
  base_url: "http://localhost:3000"

api:
  base_url: "http://localhost:8000"
  timeout: 30

database:
  connection_string: "sqlite:///qa.db"

browser:
  browser_name: "chromium"
  headless: true
  timeout: 30000