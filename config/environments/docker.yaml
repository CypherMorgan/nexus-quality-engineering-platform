environment: docker

application:
  base_url: "http://mock-app:8000"

api:
  base_url: "http://mock-app:8000"
  timeout: 30

database:
  connection_string: "sqlite:////data/users.db"

browser:
  browser_name: chromium
  headless: true
  timeout: 30000